import { create } from '@storybook/theming/create';

import logo from '../assets/logo.svg';

export default create({
  base: 'dark',
  brandTitle: 'tsdx-lib',
  brandUrl: 'https://github.com/opazupa/tsdx-lib',
  brandImage: logo,
});
